{% set name = "pygix" %}
{% set version = "2021.10.7" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: e506dd90087f939779007ededb01b8d268643616e45fc1004ed3239f345fdbb0

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - python >=3.6
    - pip
    - numpy
  run:
    - python >=3.6
    - cython
    - fabio
    - numpy
    - pyfai
    - pyyaml
    # Note: the sphinx dependencies are listed in the project's
    # `requirements.txt` file. They are only optionally used in `setup.py`, and
    # not used in the main code base. So, commenting them out:
    # - sphinx
    # - sphinxcontrib-programoutput

test:
  imports:
    - pygix
    - pygix.grazing_geometry
    - pygix.grazing_units
    - pygix.io
    - pygix.lattice
    - pygix.plotting
    - pygix.process
    - pygix.tools
    - pygix.transform
  requires:
    - pip
  commands:
    - pip check

about:
  home: https://github.com/tgdane/pygix
  summary: A python library for reduction of 2-dimensional grazing-incidence and fibre diffraction data
  license: MIT
  license_file: LICENSE
  dev_url: https://github.com/NSLS-II-SMI/pygix

extra:
  recipe-maintainers:
    - mrakitin
