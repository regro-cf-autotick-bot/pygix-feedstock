{% set name = "pygix" %}
{% set version = "2021.10.7.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: e4f0a95f126c3a0be9b133c2e5251a48668a5260a985b4945235db3e57153158

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - python >=3.6
    - pip
    - numpy
  run:
    - python >=3.6
    - cython
    - fabio
    - numpy
    - pyfai
    - pyyaml
    # Note: the sphinx dependencies are listed in the project's
    # `requirements.txt` file. They are only optionally used in `setup.py`, and
    # not used in the main code base. So, commenting them out:
    # - sphinx
    # - sphinxcontrib-programoutput

test:
  imports:
    - pygix
    - pygix.grazing_geometry
    - pygix.grazing_units
    - pygix.io
    - pygix.lattice
    - pygix.plotting
    - pygix.process
    - pygix.tools
    - pygix.transform
  requires:
    - pip
  commands:
    - pip check

about:
  home: https://github.com/tgdane/pygix
  summary: A python library for reduction of 2-dimensional grazing-incidence and fibre diffraction data
  license: MIT
  license_file: LICENSE
  dev_url: https://github.com/NSLS-II-SMI/pygix

extra:
  recipe-maintainers:
    - mrakitin
